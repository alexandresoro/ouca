import { type FunctionComponent } from "react";
import ClasseEdit from "./ClasseEdit";

const ClasseCreate: FunctionComponent = () => {
  return <ClasseEdit isEditionMode={false} />;
};

export default ClasseCreate;
