import { type FunctionComponent } from "react";
import MilieuEdit from "./MilieuEdit";

const MilieuUpdate: FunctionComponent = () => {
  return <MilieuEdit isEditionMode={true} />;
};

export default MilieuUpdate;
