import { type FunctionComponent } from "react";
import SexeEdit from "./SexeEdit";

const SexeCreate: FunctionComponent = () => {
  return <SexeEdit isEditionMode={false} />;
};

export default SexeCreate;
