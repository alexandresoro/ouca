import { type FunctionComponent } from "react";
import SexeEdit from "./SexeEdit";

const SexeUpdate: FunctionComponent = () => {
  return <SexeEdit isEditionMode={true} />;
};

export default SexeUpdate;
