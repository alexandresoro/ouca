{
  "name": "basenaturaliste-backend",
  "version": "1.0.0",
  "description": "Backend for Base Naturaliste",
  "contributors": [
    {
      "name": "Camille Carrier"
    },
    {
      "name": "Alexandre Soro"
    }
  ],
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:debug": "tsc --sourceMap true",
    "debug": "yarn build --sourceMap true && yarn start:debug",
    "dev": "yarn build && yarn start",
    "start": "node dist/backend",
    "start:debug": "node --inspect dist/backend"
  },
  "dependencies": {
    "lodash": "*",
    "mariadb": "*",
    "moment": "*"
  },
  "devDependencies": {
    "@types/lodash": "*",
    "@types/mysql": "*",
    "@types/node": "10",
    "basenaturaliste-model": "file:../basenaturaliste-model",
    "tslint": "*",
    "typescript": "*",
    "typescript-tslint-plugin": "*"
  },
  "engines": {
    "node": ">=10.0.0"
  }
}
