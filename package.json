{
  "name": "ouca-common",
  "description": "Common elements for Base Naturaliste project",
  "version": "0.0.1",
  "scripts": {
    "build": "yarn run clean && yarn run build:indexes && tsc",
    "build:indexes": "node indexes",
    "clean": "node clean",
    "deploy": "yarn run build && node deploy && yarn publish dist"
  },
  "contributors": [
    {
      "name": "Camille Carrier"
    },
    {
      "name": "Alexandre Soro"
    }
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "@types/bson": "*"
  },
  "devDependencies": {
    "typescript": "*"
  }
}
