generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Observateur {
  id Int @id @default(autoincrement()) @db.UnsignedSmallInt

  @@map("observateur")
}
